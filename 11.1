#include<iostream>
#include<string>
#include<fstream>
using namespace std;
int main()
{
	setlocale(LC_ALL, "ru");
	double chislo;
	double SumPlasElement = 0;
	int KolvoFife = 0;
	int kolvo;
	while (true)
	{
		cout << "введите 1  для записи в фаил, введите 2 для чтения из файла, для выхода введите 0 : ";
		int vibor;
		cin>>vibor;
		if (vibor == 1)
		{
			string name;
			cout << endl << "введите адрес файла или название и он создастся в этой папке" << endl;
			cin >> name;
			ofstream f;
			f.open(name);
			if (!f.is_open())
			{
				cout << "произошла ошибка" << endl;
			}
			else
			{
				cout << "фаил открылся" << endl;
				cout << "сколько вещественных чисел вы хотите записать? ";
				cin >> kolvo;
				for (int i = 0; i < kolvo; i++)
				{
					cout << "введите число номер:" << i + 1 << "\t";
					cin >> chislo;
					f << chislo << " ";
				}
			}
			f.close();
		}
		else if (vibor == 2)
		{
			string name;
			cout << endl << "введите адрес файла" << endl;
			cin >> name;
			ifstream f1;
			f1.open(name);
			if (!f1.is_open())
			{
				cout << "произошла ошибка" << endl;
			}
			else
			{
				cout << "фаил открылся" << endl;
				double r;
				while (f1 >> r)
				{
					if (r > 0)
						SumPlasElement += r;
					if (r > 1 && r == 5)
						KolvoFife++;
					cout << r << "\t";
				}
				cout << endl;
				cout << "сумма положительных элементов = " << SumPlasElement << endl;
				cout << "количество элементов == 5: " << KolvoFife << endl;
			}
			f1.close();
		}
		else if (vibor == 0)
		{
			cout << endl << "программа завершена" << endl;;
			break;
		}
		else
			cout << "вы ввели неверное число!" << endl;
			continue;
	}
	
	return 0;
}
